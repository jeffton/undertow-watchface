# Undertow
Garmin watch face

## Wake service settings
Copy `source/background/WakeServiceSettings.mc.template` to `source/background/WakeServiceSettings.mc` and fill in your own values. The real `WakeServiceSettings.mc` is ignored by git.
